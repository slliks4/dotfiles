##### ─────────────────────────────────────────────
##### 🔌  PLUGIN MANAGER + CORE PLUGINS
##### ─────────────────────────────────────────────

set -g @plugin 'tmux-plugins/tpm'
set -g @plugin 'tmux-plugins/tmux-resurrect'
set -g @plugin 'tmux-plugins/tmux-continuum'

set -g @continuum-restore 'on'
set -g @resurrect-capture-pane-contents 'on'
set -g @continuum-save-interval '15'


##### ─────────────────────────────────────────────
##### 🎨  TERMINAL / COLOR BASE
##### ─────────────────────────────────────────────

set -g default-terminal "tmux-256color"
set -as terminal-overrides ",*:Tc"

# Inherit terminal colors
set -g status-style fg=colour223,bg=default

##### ─────────────────────────────────────────────
##### 📊  STATUS BAR (RETRO AMBER)
##### ─────────────────────────────────────────────

set -g status-position top
set -g status-interval 5
set -g status-justify left

set -g status-left-length 60
set -g status-left " #[fg=colour178,bold]#S #[default]"


set -g status-right-length 40
set -g status-right "#(top -bn1 | awk '/Cpu/ {printf \"CPU %.0f%%\", $2+$4}') #(free -m | awk 'NR==2{printf \" RAM %.0f%%\", $3*100/$2 }') #[fg=colour178] %H:%M #[default]"

set -g window-status-style fg=colour180,bg=default
set -g window-status-current-style fg=colour231,bg=colour58,bold

set -g window-status-format "#I:#W "
set -g window-status-current-format " #[fg=colour231,bg=colour58,bold] #I:#W #[default] "


##### ─────────────────────────────────────────────
##### 🪟  PANES
##### ─────────────────────────────────────────────

set -g pane-border-style fg=colour58
set -g pane-active-border-style fg=colour178

set -g display-panes-time 1500
set -g display-time 1500


##### ─────────────────────────────────────────────
##### ⚙️  GENERAL BEHAVIOR
##### ─────────────────────────────────────────────

unbind C-b
set -g prefix C-Space
bind C-Space send-prefix
set -g prefix2 C-a

set -sg escape-time 0

setw -g automatic-rename on
set -g set-titles on

setw -g monitor-activity on
setw -g monitor-silence 0
setw -g monitor-bell off
set -g visual-activity off


##### ─────────────────────────────────────────────
##### ⌨️  KEYBINDINGS
##### ─────────────────────────────────────────────

unbind q
bind q detach

bind r source-file ~/.tmux.conf \; display-message "tmux.conf reloaded"
bind L clear-history

bind h select-pane -L
bind j select-pane -D
bind k select-pane -U
bind l select-pane -R

bind V split-window -h
bind H split-window -v

bind y set-window-option synchronize-panes \; display-message "synchronize-panes toggled"
bind b previous-window


##### ─────────────────────────────────────────────
##### 📋  COPY MODE
##### ─────────────────────────────────────────────

setw -g mode-keys vi

unbind p
bind p paste-buffer

bind-key -T copy-mode-vi v send -X begin-selection
bind-key -T copy-mode-vi y send -X copy-selection-and-cancel \; display-message "copied"

# Custom Popup
unbind f
bind-key f display-popup -E "$SHELL -lc ~/.local/bin/ts"

##### ─────────────────────────────────────────────
##### 🚀  PLUGIN LOADER
##### ─────────────────────────────────────────────

run '~/.config/tmux/plugins/tpm/tpm'
